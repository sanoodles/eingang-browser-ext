{
  "name": "discogs-artist-releases-on-youtube",
  "version": "1.1.0",
  "private": true,
  "description": "Chrome extension: browse a Discogs artist's releases on YouTube. (package.json is for dev tooling only; the extension itself loads from manifest.json.)",
  "scripts": {
    "test:e2e": "node test/e2e.js",
    "build": "node scripts/build.js",
    "ship": "node scripts/ship.js",
    "version": "node scripts/sync-version.js && git add manifest.json",
    "release": "npm version ${BUMP:-patch} -m \"Bump version to %s\" && npm run build && npm run ship",
    "postrelease": "echo 'Committed + tagged locally — push with: git push --follow-tags'"
  },
  "devDependencies": {
    "playwright-core": "^1.49.0"
  }
}
